<odoo>

    <record id="category_leads_form_view" model="ir.ui.view">
        <field name="name">category.leads.form.view</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
            <field name="arch" type="xml">
                <field name="team_id" position="after">
                        <field name="category"/>
                </field>
            </field>
    </record>

</odoo>